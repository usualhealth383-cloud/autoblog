# --- 1단계: 트렌드 (키 불필요) ---
requests>=2.31
feedparser>=6.0
python-dotenv>=1.0
truststore>=0.9      # 회사/보안 프록시 환경에서 HTTPS 인증서 문제 방지

# --- 3단계 이후: 본문/이미지 생성 (API 키 필요) ---
openai>=1.40
google-genai>=0.3
anthropic>=0.39

# --- 4단계: 이미지 압축 ---
pillow>=10.0

# --- 5단계: 승인 대시보드 ---
flask>=3.0

# --- 6단계: 발행 ---
google-api-python-client>=2.130
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
# 워드프레스는 REST API라 requests로 충분
